{
    "formFields":
        {
            "code":{ "type":"text", "required":true, "maxLength":20, "pattern":null, "validationMessage":"Field is either empty or incorrect"},
            "name":{ "type":"text", "required":true, "maxLength":100, "pattern":null, "validationMessage":"Field is either empty or incorrect"},
            "description":{ "type":"textarea", "required":false, "maxLength":5000, "pattern":null, "validationMessage":"Field is either empty or incorrect"}
        },

    "columnDefs": [
        {
            "headerName": "Code",
            "field": "code",
            "sort": "asc"            
        },
        {
            "headerName": "Name",
            "field": "name"
        }
    ],
    "operations": [        
        {
            "name": "Add New",
            "id": "add"
        }     
    ],
    "advanceSearch": 
        {
            "fields": [
                {
                    "label": "Name",
                    "field": "name",
                    "type": "text"
                }
            ]

        }
}